## Packages
recharts | Dashboard analytics charts
framer-motion | Page transitions and animations
date-fns | Date formatting for tables
lucide-react | Icons (already in base but explicit check)
clsx | Class name utility (already in base)
tailwind-merge | Class name utility (already in base)

## Notes
- Authenticated app using Replit Auth
- File uploads use POST /api/documents with FormData
- Locked patients cannot be edited unless user is admin
- Theme: Clean, professional medical dashboard (Blue/Teal/White)
